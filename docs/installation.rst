============
Installation
============

At the command line::

    $ easy_install django-answerdiff

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-answerdiff
    $ pip install django-answerdiff
